export { default as ConfigProvider } from './ConfigProvider';
export { useConfigStore, initLocale } from './store';
export type {
  ConfigProviderProps,
  Locale,
  LocaleMessages,
  ConfigStore,
} from './types';
